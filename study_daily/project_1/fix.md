# fix project 1
```
Q2b：通过统计检验来辩证提出的假设，并针对统计检验的假设前提进行说明
这一题要求的是统计检验的类型，最为常用的是t检验和z检验，这个链接列出了更多类型。
从第5题来看，你选择的是相依样本的t检验，这是最为适合本实验的一种。
需要注意的是双样本t检验和相依样本t检验不是一回事，可以参考t 检验的类型。
那么，为什么是相依样本呢？为什么是t检验呢？可参考以下判断流程图：
t-score-vs.-z-score.png

本题还要求说明所选统计检验的假设前提(assumptions)。如果违背了这些条件，则可能影响检验结果的可信度。
比如，有些统计检验要求数据服从正态分布。
要进一步探索这个问题，可以参与论坛讨论，也可以参考这个链接和这个链接。


Q5：正确执行并报告了一项统计检验，包括检验统计量、P 值和检验结果。检验结果针对执行的试验任务进行了解释。
你的t-statistic结果显示，你可能使用了双样本（或独立样本）t检验的计算方法，用合并标准差的公式计算差值样本的标准差。
什么是合并标准差？
提到
合并标准差是一种用来估计单个标准差以表示所有独立样本的方法
前提是假定这些样本所在的总体具有相同的标准差
那么，本实验中的两个样本满足这些条件吗？
可以参考这个链接，了解正确的计算方法。


1. 增加Q2a的假设集的文字描述：
  1. 零假设：文字条件一致与不一致导致的阅读列表所花时间无明显区别；
  2. 对立假设：文字条件一致与不一致导致的阅读列表所花时间有明显区别；
  3. mu_congruent：表示文字条件一致的样本均值；
  4. mu_incongruent：表示文字条件不一致的样本均值；
2. Q2b:
  1. 相依样本的t检验，首先我们并没有总体的数据，因此无法得到总体均值，而样本量小于30，因此选择t检验，
  又因为我们的样本并非是两组独立样本，而是同一组样本在不同条件下的实验，因此是相依样本；
  2. 相依样本的t检验的前提：
    1. 正态分布的总体（即使不满足正态分布，通常t也是有效的，但是只针对分布偏斜不大时）；
    2. 针对同一样本不同条件下的观察；
3. 执行统计检验：
  问题：不应该使用合并标准差来计算，因为合并标准差适用于估计单个标准差以表示研究中所有独立样本或组的方法，且需要样本所在总体具有相同的标准差；
  正确计算流程：
    1. 
```
